pipeline{
  agent any{
    stages{
      stage("build"){
        steps{ echo 'building the app!!' }
    }
      stage("test"){
        steps{ echo 'Testing the app :P'}
          }
      stage('deploy'){
        steps{echo 'Deploying the app :)'}
          }
              }
  }
}
